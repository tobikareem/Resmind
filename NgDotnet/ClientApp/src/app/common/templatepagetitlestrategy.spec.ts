import { Templatepagetitlestrategy } from './templatepagetitlestrategy';

describe('Templatepagetitlestrategy', () => {
  it('should create an instance', () => {
    expect(new Templatepagetitlestrategy()).toBeTruthy();
  });
});
